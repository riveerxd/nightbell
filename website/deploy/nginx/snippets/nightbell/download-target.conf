# Generated by scripts/gen-download-redirect.mjs. Do not edit.
#
# The target of /download, regenerated from RELEASE in site.config.mjs on every
# build and checked by `npm run verify`, so it cannot fall behind a release.
#
# 302 rather than 301: this target changes with every version, and a permanent
# redirect would be cached in browsers past the next release with no request
# reaching this server to correct it.
#
# Included from the `location = /download` block. It sets the caching and the
# logging; this file only says where the click goes.
#
#   version   3.8.0
#   asset     Nightbell-3.8.0-release.apk
return 302 https://github.com/riveerxd/nightbell/releases/download/v3.8.0/Nightbell-3.8.0-release.apk;
