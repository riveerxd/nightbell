<?xml version="1.0" encoding="utf-8"?>
<!-- One monitor. Added dynamically via RemoteViews.addView so a widget can
     show as many rows as its configuration allows without eight dead stubs. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/row_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="5dp"
    android:paddingBottom="5dp">

    <ImageView
        android:id="@+id/row_dot"
        android:layout_width="9dp"
        android:layout_height="9dp"
        android:layout_marginEnd="10dp"
        android:src="@drawable/widget_dot"
        android:contentDescription="@null" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/row_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:textSize="13sp"
            android:textColor="#FFFFFFFF" />

        <TextView
            android:id="@+id/row_meta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:textSize="10sp"
            android:visibility="gone"
            android:textColor="#FF8A8A8A" />
    </LinearLayout>

    <TextView
        android:id="@+id/row_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:maxLines="1"
        android:textSize="11sp"
        android:textColor="#FFD6D6D6" />

    <!-- Follows the value, for a repository row: "13 ★". Gone on every other
         kind of monitor, so a latency reading keeps the whole trailing slot. -->
    <ImageView
        android:id="@+id/row_value_icon"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_marginStart="3dp"
        android:src="@drawable/widget_star"
        android:visibility="gone"
        android:contentDescription="@null" />
</LinearLayout>
